{
    "patterns": [
        {"name": "AWS Access Key", "regex": "AKIA[0-9A-Z]{16}"},
        {"name": "AWS Secret Key", "regex": "aws_secret_access_key\\s*[:=]\\s*[0-9a-zA-Z/+]{40}"},
        {"name": "Generic API Key", "regex": "[A-Za-z0-9_]{32}"}
    ]
}
